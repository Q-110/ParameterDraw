{
  "name": "parameter-draw-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"vite --host 127.0.0.1 --strictPort\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "build": "vite build",
    "backend:build": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-python-backend.ps1",
    "dist": "pnpm backend:build && pnpm build && electron-builder --config electron-builder.config.cjs --win nsis portable",
    "dist:portable": "pnpm backend:build && pnpm build && electron-builder --config electron-builder.config.cjs --win portable",
    "dist:installer": "pnpm backend:build && pnpm build && electron-builder --config electron-builder.config.cjs --win nsis",
    "dist:test": "pnpm backend:build && pnpm build && set PARAMETER_DRAW_INCLUDE_TEMPLATES=true&& electron-builder --config electron-builder.config.cjs --win portable"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.2",
    "element-plus": "^2.14.1",
    "three": "^0.184.0",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "concurrently": "^10.0.1",
    "electron": "^42.3.0",
    "electron-builder": "^26.8.1",
    "less": "^4.6.4",
    "vite": "^8.0.12",
    "wait-on": "^9.0.10"
  }
}
